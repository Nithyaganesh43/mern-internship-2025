css :
    translate
    rotate
    scale x,y
    skew -x,y
    matrix


translate:
    move element from one porition to anothe position
translate()
Moves an element along the X and Y axes.
Example: transform: translate(50px, 100px);

translateX()
Moves an element along the X-axis.
Example: transform: translateX(50px);

translateY()
Moves an element along the Y-axis.
Example: transform: translateY(100px);

translateZ()
Moves an element along the Z-axis (3D transformations).
Example: transform: translateZ(200px);

translate3d()
Moves an element in 3D space.
Example: transform: translate3d(50px, 100px, 200px);

scale()
Resizes an element uniformly along the X and Y axes.
Example: transform: scale(2);

scaleX()
Resizes an element along the X-axis.
Example: transform: scaleX(1.5);

scaleY()
Resizes an element along the Y-axis.
Example: transform: scaleY(0.5);

scaleZ()
Resizes an element along the Z-axis (3D transformations).
Example: transform: scaleZ(1.2);

scale3d()
Resizes an element in 3D space.
Example: transform: scale3d(2, 1.5, 1);

rotate()
Rotates an element around the origin in 2D space.
Example: transform: rotate(45deg);

rotateX()
Rotates an element around the X-axis.
Example: transform: rotateX(45deg);

rotateY()
Rotates an element around the Y-axis.
Example: transform: rotateY(45deg);

rotateZ()
Rotates an element around the Z-axis (same as rotate() for 2D).
Example: transform: rotateZ(45deg);

rotate3d()
Rotates an element in 3D space around a specified axis.
Example: transform: rotate3d(1, 1, 0, 45deg);

skew()
Skews an element along the X and Y axes.
Example: transform: skew(30deg, 20deg);

skewX()
Skews an element along the X-axis.
Example: transform: skewX(30deg);

skewY()
Skews an element along the Y-axis.
Example: transform: skewY(20deg);

perspective()
Applies perspective to a 3D-transformed element.
Example: transform: perspective(1000px);


transision :
     transition: property duration timing-function delay;

    properties
        else
        linear
        else in
        else in out


animations:
    key frams
    animation name
    animation duration